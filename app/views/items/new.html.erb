<a href="/"><img id="logo" src=<%= image_path('logo.png') %>/></a>

<h1>Add a new item</h1>

<% if user_signed_in? %>
  <p>
    <%= form_for @item do |f| %>
      <%= f.text_field :content %>
      <%= f.submit "Create" %>
    <% end %>
  </p>
<% end %>
